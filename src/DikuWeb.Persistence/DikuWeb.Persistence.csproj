<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\DikuWeb.Domain\DikuWeb.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- SnakeCaseNaming is a convention rather than an entity, so the only honest way to test it
         is directly, against a model built for the purpose. Matches Engine and Server. -->
    <InternalsVisibleTo Include="DikuWeb.Server.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

</Project>
