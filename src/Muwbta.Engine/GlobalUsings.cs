// Randomness lives in Domain (PLAN.md §9) but is used throughout the Engine, so it is
// imported globally rather than repeated in every system that rolls dice.
global using Muwbta.Domain.Randomness;
