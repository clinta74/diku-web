# Play testing notes

Add anything noticed while playing here. Cleared as items are done.

- A admin should be able to trigger a shutdown of the server with a command. This can be a time delay with a warning and a time value provided. a delay of a 0 would force an instant shutdown. Maybe this should be in the builder so that we can confirm the shutdown.
